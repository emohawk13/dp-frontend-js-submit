Replicate the Random Student Generator’s layout. Though appropriate colors are important to the design they do not need to match the original generator’s color scheme.

NOTE: Remember to follow the spiral design method. Program each aspect of the program manually. Once the entire program works, refractor to make your program DRYer. Continue until you have a succinct program.

Create the following in your design:

A Sidebar, which should:
Fetch your name from the api so it is displayed
Fetch the list of all users
Render the names of all users

A Main Section
Should have a generic placeholder message upon loading
A "Randomizer" button that will select a random user and will display their name in place of the holder.

Your design will use CSS of the following:

Animations
Have 1 (one) hover effect
Style each aspect of the design including buttons, inputs, fonts, colors
At least 1 (one) added font
At least 1 (one) icon (this could be a loading icon that spins or anything of your choice

Your design should use JS for the following:

The names should display being shuffled when choosing a random user
Ensure proper name formatting
When a user is seleected their name should highlight in the sidebar

Here is the API Endpoint for your use: GET: https://fe-students.onrender.com/api/users

ADDITIONAL PRACTICE: Weight the list of users. Add buttons in a way that adds and removes weight from each user. Create a legend or key that explains the weight system including how to gain or lose weight.
Provide a link to a git repository in the field below:
