1st Challenge
Make a function that will dynamically create a heading tag between h1-h6. Your function should:
take two properties: heading size and heading text
return a heading element with the text included

Write a render function that will append an element to the Document Object Module (DOM)
ADDITIONAL PRACTICE: Use a class to append an element to the DOM.
2nd Challenge
Create a dynamic navigation bar

The navigation bar should include images, links and sections.
Make the navigation bar responsive with the following:
When the page hits a media query breakpoint, the navigation bar will turn into a "hamburger menu".
A user should click on the hambruger menu icon to display the dropdown menu.
NOTE: work on this assignment in pieces. First create the html skeleton (includign the elements), style it, create the media query and then create the js programming.
NOTE: Keep yourself moving. If you find yourself stuck, ask someone for help or move on to another portion of your code.
Provide a link to a git repository or codepen in the field below:
